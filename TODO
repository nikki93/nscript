Todo
----
- Add license.

- Format string output.

- Make all the 'obj.bla' stuff use the macros instead.

- Implement 'symbol' idea?

- Use an array for stack instead of linked list?

- Add 'array' type?

- Add structs?

- Move everything into 'nsvm' struct to allow multiple nscript vms in
  the same program?

